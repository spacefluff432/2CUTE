{
   "default": {
      "class": "XRenderer",
      "debug": false,
      "range": {
         "minimum": 0,
         "maximum": 0
      }
   },
   "description": "A Storyteller renderer.",
   "properties": {
      "class": {
         "default": "XRenderer",
         "description": "The internal class represented by this file.",
         "enum": [
            "XRenderer"
         ],
         "type": "string"
      },
      "debug": {
         "default": false,
         "description": "Enables this renderer's debug mode.",
         "type": "boolean"
      },
      "range": {
         "default": {
            "minimum": 0,
            "maximum": 0
         },
         "properties": {
            "minimum": {
               "default": 0,
               "description": "The minimum Z axis an entity must have to render.",
               "type": "number"
            },
            "maximum": {
               "default": 0,
               "description": "The maximum Z axis an entity must have to render.",
               "type": "number"
            }
         }
      }
   },
   "required": [
      "class"
   ],
   "type": "object"
}